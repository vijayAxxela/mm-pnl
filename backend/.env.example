# Copy to backend/.env and fill in — required (Postgres only, no local
# fallback). If unset, the app falls back to database.url in config.json.
#
# DATABASE_URL=postgresql+psycopg2://<user>:<password>@localhost:5432/<dbname>
